import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    cont: {
      display: "flex",
      alignItems: "center",
      flexDirection: "column",
      marginTop: 40,
    },
  });

export default styles;
