import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    levelSelect: {
      backgroundColor: "#A9A9A9",
      width: 50,
      marginLeft: 10,
      textAlign: "center",
      pointerEvents: "none",
      fontSize: 18,
      paddingLeft: 15,
      color: "aliceblue",
    },
  });

export default styles;
